{
  "$comment": "Neutral MCP server list, generated into each tool's config. stdio: {command, args, env}; remote: {url}. Values use ${VAR} for secrets, never literals. Empty until a role needs a server: Playwright arrives with the designer and UI reviewer (2.4), the Jira tracker with 2.6.",
  "servers": {}
}
