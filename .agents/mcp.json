{
  "$comment": "Neutral MCP server list, generated into each tool's config. stdio: {command, args, env}; remote: {url}. Values use ${VAR} for secrets, never literals. Roles reference a server as the capability mcp:<name>. The Jira tracker server arrives with Segment 2.6.",
  "servers": {
    "playwright": {
      "description": "Browser automation for ux-designer (capture current UI) and frontend-engineer / ui-reviewer (check rendered states). Local only; no credentials.",
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    }
  }
}
