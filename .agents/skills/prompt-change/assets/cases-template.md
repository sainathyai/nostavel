# Assistant cases

Recorded guest phrasings and what the assistant should make of them. Every prompt or
schema change is judged against this file, before and after (`prompt-change` skill).

Keep cases that pass as well as ones that fail: the passing ones are what catch a
regression.

## How to run them

<the command, once it exists; until then say how they were run by hand>

## Cases

| # | Guest says | Context (date, current search) | Expected | Last result | Checked |
|---|---|---|---|---|---|
| 1 | | | | | YYYY-MM-DD |

### Notes per case

**Case 1**
- **Why it matters:** <the guest harm if this is wrong>
- **Actual output (last run):**
  ```json
  ```
- **Status:** passing | failing | ambiguous
- **Became a test:** <test file, or "not yet">
