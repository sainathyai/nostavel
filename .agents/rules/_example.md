---
name: example-area
description: What this rule covers. Files starting with "_" are schema examples and are never generated.
globs:
  - "src/lib/**/*.ts"
---

Short, imperative rules for anyone (human or agent) editing files matching the globs.
Summarise and link the relevant `docs/conventions.md` section rather than restating it,
so the conventions stay the single source of truth.
